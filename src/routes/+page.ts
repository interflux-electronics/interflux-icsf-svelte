import type { PageLoad } from './$types';

export const load: PageLoad = ({ params }) => {
  // TODO redirect to /en or /ja based on header
  // return {
  //   locale: 'en',
  //   translations: [],
  //   seo: {
  //     title: "ICSF Select6 – World's most intelligent selective jet fluxer."
  //   }
  // };
};
